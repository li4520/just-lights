# Security Policy

Just Lights is a local Garmin Connect IQ data field. It does not use a backend service and should not require secrets at runtime.

Do not publish Connect IQ developer keys, signing material, debug XML, or local simulator data in issues or pull requests.

For a private report, contact support@windblows.cc with the subject `Just Lights security`.
